#include <iostream>
#include <algorithm>
using namespace std;
int n,l,r,j;
pair<int,int> a[200000];
int main()
{
    for(int i=0;i<n;i++)
    {
        cin>>a[i].first>>a[i].second;
    }
    sort(a,a+n)
    for(int i=0;i<n;i++)
    {
        j
    }

    return 0;
}
