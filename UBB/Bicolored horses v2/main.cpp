#include <iostream>

using namespace std;
int n,k,a[501],sol=1000000000;
int cmin[501][501];
int main()
{
    cin>>n>>k;
    for(int i=1;i<=n;++i)cin>>a[i];
    i->grajduri
    j->cai
    cmin[i][j]=min(cmin[i][j],cmin[i-1][l]+cost(l+1...j));
    cost(l+1...j)=alb(l+1...j)*negru(l+1...j);
    return 0;
}
